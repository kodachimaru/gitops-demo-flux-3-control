---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-deployment
  namespace: staging
  annotations:
    fluxcd.io/automated: 'true'
    fluxcd.io/tag.nginx: glob:1.8.*
    fluxcd.io/locked_user: sosuna
    fluxcd.io/locked: 'true'
    fluxcd.io/locked_msg: Performance problems, fix in progress
spec:
  selector:
    matchLabels:
      app: nginx
  replicas: 2
  template:
    metadata:
      labels:
        app: nginx
    spec:
      containers:
      - name: nginx
        image: nginx:1.7.9
        ports:
        - containerPort: 80
